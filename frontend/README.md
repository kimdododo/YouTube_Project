# YouTube 데이터 파이프라인 대시보드

React + Vite 기반의 YouTube 데이터 수집 파이프라인 모니터링 대시보드입니다.

## 기능

- 📊 파이프라인 상태 모니터링
- 📈 데이터 수집 통계
- 📋 최근 실행 내역
- 💾 데이터베이스 연결 상태
- 📉 데이터 수집 추이 차트

## 시작하기

### 설치

```bash
cd frontend
npm install
```

### 개발 서버 실행

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속

### 빌드

```bash
npm run build
```

## 프로젝트 구조

```
frontend/
├── src/
│   ├── components/
│   │   ├── Dashboard.jsx       # 메인 대시보드
│   │   ├── StatCard.jsx         # 통계 카드
│   │   ├── PipelineStatus.jsx   # 파이프라인 상태
│   │   ├── RecentRuns.jsx       # 최근 실행 내역
│   │   └── DataChart.jsx        # 데이터 차트
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── package.json
├── vite.config.js
└── tailwind.config.js
```

## 사용 기술

- React 18
- Vite
- Tailwind CSS
- Recharts (차트)
- Lucide React (아이콘)
- date-fns (날짜 포맷)

## API 연동

현재는 임시 데이터를 사용하고 있습니다. 실제 API와 연동하려면:

1. `src/components/Dashboard.jsx`의 `fetchStats` 함수 수정
2. API 엔드포인트 설정
3. 환경 변수 설정 (필요시)

## 커스터마이징

### 색상 변경

`tailwind.config.js`에서 primary 색상을 변경할 수 있습니다.

### 컴포넌트 수정

각 컴포넌트는 독립적으로 수정 가능합니다:
- `StatCard`: 통계 카드 스타일
- `PipelineStatus`: 파이프라인 상태 표시
- `RecentRuns`: 실행 내역 테이블
- `DataChart`: 차트 타입 및 데이터

