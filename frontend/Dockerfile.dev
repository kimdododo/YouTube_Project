FROM node:18-alpine

WORKDIR /app

# package.json 복사 및 의존성 설치
COPY package.json package-lock.json* ./
RUN npm install

# 소스 코드 복사
COPY . .

# 개발 서버 실행 (--host 플래그로 모든 인터페이스에서 접근 가능)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

